---

# Lab 3 — Add DTOs and Validation

## Objective

Improve the API by separating the API request model from the internal Student object.

---

# Step 1 — Create DTO Package

Create:

```text
dto
```

Create:

```text
CreateStudentRequest.java
```

---

# Step 2 — Create DTO

```java
public class CreateStudentRequest {

    private String name;
    private String email;
    private String course;

    // getters and setters
}
```

---

# Step 3 — Change POST API

Instead of:

```java
@RequestBody Student student
```

use:

```java
@RequestBody CreateStudentRequest request
```

Example:

```java
@PostMapping("/students")
public Student createStudent(
        @RequestBody CreateStudentRequest request) {

    Student student = new Student();

    student.setId(101L);
    student.setName(request.getName());
    student.setEmail(request.getEmail());
    student.setCourse(request.getCourse());

    return student;
}
```

---

# Step 4 — Add Validation Dependency

Open:

```text
pom.xml
```

Add Spring Boot validation:

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>
```

Save the file.

Reload Maven.

---

# Step 5 — Add Validation

Update DTO:

```java
import jakarta.validation.constraints.Email;
import jakarta.validation.constraints.NotBlank;

public class CreateStudentRequest {

    @NotBlank
    private String name;

    @NotBlank
    @Email
    private String email;

    @NotBlank
    private String course;

    // getters and setters
}
```

---

# Step 6 — Enable Validation

Update controller:

```java
@PostMapping("/students")
public Student createStudent(
        @Valid @RequestBody CreateStudentRequest request) {

    ...
}
```

Import:

```java
import jakarta.validation.Valid;
```

---

# Step 7 — Test Valid Request

```json
{
  "name": "Rahul",
  "email": "rahul@example.com",
  "course": "Spring Boot"
}
```

Expected:

```text
Success
```

---

# Step 8 — Test Invalid Request

Send:

```json
{
  "name": "",
  "email": "abc",
  "course": ""
}
```

Expected:

```text
400 Bad Request
```

---

# Step 9 — Experiment

Try each individually.

### Empty name

```json
{
  "name": "",
  "email": "rahul@example.com",
  "course": "Java"
}
```

### Invalid email

```json
{
  "name": "Rahul",
  "email": "abc",
  "course": "Java"
}
```

### Empty course

```json
{
  "name": "Rahul",
  "email": "rahul@example.com",
  "course": ""
}
```

Record the responses.

---

# Lab 3 Checkpoint

You should now understand:

```text
JSON
 ↓
DTO
 ↓
Validation
 ↓
Controller
```